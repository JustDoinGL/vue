export enum Links {
  LOGIN = `/`,
  POSTS = `/posts`,
  POST = `/post/:id`,
  USERS = '/users'
}

export enum PathNames {
  LOGIN = 'Profile',
  POSTS = 'Posts',
  POST = 'Post',
  USERS = 'Users'
}
